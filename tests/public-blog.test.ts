import test from "node:test";import assert from "node:assert/strict";import { canonicalBlogUrl,isSafeHref,isSupportedContentNode,metadataForPost,normalizePage,normalizeSearch,publicPostFilter } from "../lib/blog/public.ts";
test("public visibility excludes drafts archived and future posts",()=>{const now=new Date("2026-01-01T00:00:00Z");const rows=[{status:"published",published_at:"2025-01-01T00:00:00Z",id:1},{status:"draft",published_at:null,id:2},{status:"archived",published_at:"2025-01-01T00:00:00Z",id:3},{status:"published",published_at:"2027-01-01T00:00:00Z",id:4}];assert.deepEqual(publicPostFilter(rows,now).map(x=>x.id),[1])});
test("metadata uses fallbacks and correct canonical",()=>{const m=metadataForPost({title:"Title",excerpt:"Excerpt",slug:"tajweed",seo_title:null,seo_description:null,featured_image_path:null});assert.equal(m.title,"Title");assert.equal(m.description,"Excerpt");assert.equal(m.canonical,"https://www.alsheerazislamicschool.com/blog/tajweed");assert.equal(canonicalBlogUrl(),"https://www.alsheerazislamicschool.com/blog")});
test("search and pagination parameters are normalized",()=>{assert.equal(normalizeSearch("  tajweed%,()  "),"tajweed");assert.equal(normalizePage("2"),2);assert.equal(normalizePage("-2"),1);assert.equal(normalizePage("oops"),1)});
test("structured content whitelist fails unknown nodes safely",()=>{for(const type of ["doc","paragraph","heading","bulletList","orderedList","blockquote","horizontalRule"])assert.equal(isSupportedContentNode(type),true);assert.equal(isSupportedContentNode("script"),false)});
test("links allow safe protocols and reject executable schemes",()=>{assert.equal(isSafeHref("https://example.com"),true);assert.equal(isSafeHref("/contact"),true);assert.equal(isSafeHref("mailto:info@example.com"),true);assert.equal(isSafeHref("javascript:alert(1)"),false);assert.equal(isSafeHref("data:text/html,bad"),false)});
